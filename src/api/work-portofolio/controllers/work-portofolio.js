'use strict';

/**
 * work-portofolio controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::work-portofolio.work-portofolio');
